seed -Whipe db, reset it up. Usually used when re-running an updated seed file

```rails db:setup```

Rake routes for a specific set of routes

```rake routes | grep portfolio```

Updating a gem

```bundle update factory_girl```

Starting New App

```rails new portfolio_aw -T --database=postgresql```

Creates a new database - has to be done after creating a new app

```rails db:create```

